*volume=0
<(a * b) --> like>.
<(b * a) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(c * d) --> like>.
<(d * c) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(e * f) --> like>.
<(f * e) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(g * h) --> like>.
<(h * g) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(i * j) --> like>.
<(j * i) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(k * l) --> like>.
<(l * k) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(m * n) --> like>.
<(n * m) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(o * p) --> like>.
<(p * o) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(q * r) --> like>.
<(r * q) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(s * t) --> like>.
<(t * s) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(u * v) --> like>.
<(v * u) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
6000
<(w * x) --> like>.
<(x * w) --> like>.
<<($1 * $2) --> like> ==> <($2 * $1) --> like>>?
100
<(y * z) --> like>.
10
<(z * y) --> like>?
//expected: Answer: <(z * y) --> like>. Truth: frequency=1.000000, confidence=0.866876
